import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: [
      `image.tmdb.org`,
      'via.placeholder.com',
      'placehold.co'

    ], 
  },
};

export default nextConfig;